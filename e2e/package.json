{
  "name": "ziv-e2e",
  "private": true,
  "description": "Browser tests for ziv's built-in viewer. Standalone, outside the cargo workspace.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
